// Relocated to @wivwav/db (#951); shim until the #948 cutover.
// FieldResolutionState comes from @wivwav/types (its canonical home).
export type { ClaimField, EvidenceKind, FieldClaim, NewFieldClaim, ResolvedField } from '@wivwav/db'
export type { FieldResolutionState } from '@wivwav/types'
