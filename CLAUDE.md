# Claude Instructions

Read and follow `AGENTS.md` in this repository root as the canonical project guide and workflow.

Claude-specific instructions may live here before this handoff. When invoked through a file in
`.claude/commands/`, follow that command's additional instructions together with `AGENTS.md`.

If a Claude-specific instruction conflicts with `AGENTS.md` only because of Claude tooling or slash
command behavior, use the Claude-specific instruction. Otherwise, treat `AGENTS.md` as the source of
truth for project architecture, development workflow, testing, commits, and PR expectations.
